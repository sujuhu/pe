
SET(SRC_LIST
petool.cpp)

ADD_EXECUTABLE(petool ${SRC_LIST})
TARGET_LINK_LIBRARIES(petool pe_static)
TARGET_LINK_LIBRARIES(petool filemap)
IF(WIN32)
TARGET_LINK_LIBRARIES(petool version)
ENDIF(WIN32)

#install
INSTALL(TARGETS petool RUNTIME DESTINATION bin)