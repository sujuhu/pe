PROJECT(pe)
	
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
INCLUDE_DIRECTORIES(${CMAKE_INSTALL_PREFIX}"\\include")
LINK_DIRECTORIES(${CMAKE_INSTALL_PREFIX}"\\lib")

ADD_SUBDIRECTORY(src build)
ADD_SUBDIRECTORY(pywrap pybuild)